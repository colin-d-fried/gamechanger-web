// Stub used by Jest's moduleNameMapper for static assets (images, fonts, pdfs).
// Returning an empty string means `import x from './foo.png'` resolves to `''`
// during tests, which is enough for assertions on component rendering.
module.exports = '';
